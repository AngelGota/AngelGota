name: Auto PR para todas las feature/*

on:
  schedule:
    - cron: "*/6 * * * *"  # cada 10 min (UTC)
  workflow_dispatch:
  push:
    branches:
      - 'feature/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  open-prs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Crear PRs faltantes (feature/* → main)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # gh usa GH_TOKEN o GITHUB_TOKEN
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
        
          # Listar ramas remotas feature/*
          mapfile -t BRANCHES < <(git ls-remote --heads "https://github.com/${REPO}.git" 'refs/heads/feature/*' \
                                | awk '{print $2}' | sed 's@refs/heads/@@')
        
          for HEAD in "${BRANCHES[@]}"; do
            # 1) ¿Ya hay PR abierto? → comprobar por **salida**, no por exit code
            EXISTE=$(gh pr list --repo "${REPO}" --head "${HEAD}" --base "main" --state open \
                       --json number --jq '.[0].number' || true)
            if [[ -n "${EXISTE}" ]]; then
              echo "✔ PR ya existe para ${HEAD} (#${EXISTE})"
              continue
            fi
        
            # 2) ¿Hay diff real contra main?
            git fetch --no-tags --quiet origin "${HEAD}" main
            # commits únicos en la rama (lado derecho del triple-dot)
            AHEAD=$(git rev-list --left-right --count "origin/main...origin/${HEAD}" | awk '{print $2}')
            if [[ "${AHEAD}" -eq 0 ]]; then
              echo "⏭ Sin cambios vs main en ${HEAD}"
              continue
            fi
        
            # 3) Crear PR
            echo "↗ Crear PR para ${HEAD}"
            URL=$(gh pr create \
              --repo "${REPO}" \
              --base main \
              --head "${HEAD}" \
              --title "feat: ${HEAD} → main" \
              --body "PR automático generado por auto-pr." \
              --label "needs-review" --label "auto-pr" \
              --json url --jq .url)
        
            echo "✅ PR creado: ${URL}"
          done
