name: 3. Auto review + AutoCommentGotaBot + AutoApprove

on:
  schedule:
    - cron: "*/5 * * * *"   # cada 5 minutos (GitHub Actions no permite menos)
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize]
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: write
  checks: read
  statuses: read

jobs:
  on-pr-event:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      # Esperar a que los checks terminen correctamente
      - name: Esperar checks OK
        uses: lewagon/wait-on-check-action@v1.3.3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          wait-interval: 10
          allowed-conclusions: success,neutral,skipped
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      # Dejar comentario genérico GotaBot
      - name: Comentario GotaBot
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          gh pr comment "$PR" --repo "$REPO" --body $'🤖 **GotaBot – Generic Review**\n• CI ✓\n• Base: `main`\n• Autor: @${{ github.actor }}\n\n> Revisión automática completada.'

      # Validar si hay "changes requested"
      - name: Bloquear si hay CHANGES_REQUESTED
        id: revs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          CR=$(gh pr reviews --repo "$REPO" "$PR" --json state --jq '[.[].state] | any(.=="CHANGES_REQUESTED")')
          echo "changes=$CR" >> $GITHUB_OUTPUT
          [[ "$CR" == "true" ]] && echo "Hay CHANGES_REQUESTED; no se aprobará."

      # Aprobar con tu usuario real (GH_PAT)
      - name: Aprobar como AngelGota
        if: steps.revs.outputs.changes == 'false'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}   # tu PAT (fine-grained o classic con repo scope)
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          gh pr review "$PR" --repo "$REPO" --approve --body "Auto-approval by AngelGota (CI OK)."

  # ========= Ejecución manual para PRs abiertos =========
  backfill-open-prs:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_PAT: ${{ secrets.GH_PAT }}
      REPO: ${{ github.repository }}
    steps:
      - name: Listar PRs abiertos
        id: list
        run: |
          gh pr list --repo "$REPO" --state open --json number --jq '.[].number' > prs.txt || true
          echo "prs<<EOF" >> $GITHUB_OUTPUT; cat prs.txt >> $GITHUB_OUTPUT; echo "EOF" >> $GITHUB_OUTPUT
          echo "Open PRs:"; cat prs.txt || true

      - name: Procesar PRs abiertos
        if: steps.list.outputs.prs != ''
        run: |
          set -euo pipefail

          wait_checks () {
            local sha="$1" tries=0 max=120
            while :; do
              state=$(gh api repos/${REPO}/commits/${sha}/status --jq .state)
              echo "Checks state: $state"
              [[ "$state" == "success" ]] && return 0
              [[ "$state" == "failure" ]] && { echo "Checks failed"; return 1; }
              ((tries++)); [[ $tries -ge $max ]] && { echo "Timeout"; return 1; }
              sleep 5
            done
          }

          while read -r PR; do
            [[ -z "$PR" ]] && continue
            echo "== PR #$PR =="

            gh pr comment "$PR" --repo "$REPO" --body $'🤖 **GotaBot – Generic Review**\n• Backfill\n\n> Esperando checks...'

            SHA=$(gh pr view "$PR" --repo "$REPO" --json headRefOid --jq .headRefOid)
            wait_checks "$SHA"

            CR=$(gh pr reviews --repo "$REPO" "$PR" --json state --jq '[.[].state] | any(.=="CHANGES_REQUESTED")')
            if [[ "$CR" == "true" ]]; then
              gh pr comment "$PR" --repo "$REPO" --body "🔎 GotaBot: *changes requested* detectado. No se aprueba."
              continue
            fi

            GH_TOKEN="$GH_PAT" gh pr review "$PR" --repo "$REPO" --approve --body "Auto-approval by AngelGota (backfill)."
            gh pr comment "$PR" --repo "$REPO" --body "✅ GotaBot: Checks OK y aprobado por AngelGota."
          done < prs.txt
